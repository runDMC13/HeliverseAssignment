<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HeliverseAssignment" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowFullscreen">true</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="buttonStyle">@style/BlueButton</item>
    </style>

    <style name="BlueButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_selector</item>
        <item name="android:textColor">#fff</item>
        <item name="android:paddingLeft">24dp</item>
        <item name="android:paddingRight">24dp</item>
    </style>

    <style name="Theme.HeliverseAssignment" parent="Base.Theme.HeliverseAssignment" />
</resources>